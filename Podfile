# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ML_AR_UI_Kit' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ML_AR_UI_Kit
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'SDWebImage'

  target 'ML_AR_UI_KitTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ML_AR_UI_KitUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
